;jQuery(document).ready(function($){      /*  $('#switchify').html('<select><option value="1">On</option><option value="0">Off</option></select>');        $('select:not(.args, .evented)').switchify();        $('select.args').switchify({ on: '1', off: '0' });        $('.api').click(function(e) {            e.preventDefault();            var self = $(this),                $switch = $('#api-select').data('switch'),                controls = $switch.data('controls');                controls[self.attr('data-action')]();        });*/    var params = {};    if (location.search) {        var parts;        parts = location.search.substring(1).split('&');        var i;        for (i = 0; i < parts.length; i++) {            var nv;            nv = parts[i].split('=');            if (!nv[0]) continue;            params[nv[0]] = nv[1] || true;        }        //salert(params.deviceToken);        localStorage.setItem('deviceToken',params.deviceToken);        localStorage.setItem('registeredForPush',params.registeredForPush);        localStorage.setItem('UUID',params.UUID);    }    /*$("[data-role=page]").css("display", "none");    $("[data-role=page]").fadeIn(500,function(){        $(".preloader").fadeOut(1000);    });*/    $(".preloader").fadeOut(1000);    $("a:not(#settings)").click(function(event){        event.preventDefault();        if(this.href.indexOf('#') == -1) {            linkLocation = this.href;            $(".preloader").fadeIn(1000,function(){                $("[data-role=page]").fadeOut(500, redirectPage);            });        }    });    function redirectPage() {        window.location = linkLocation;    }});/*window.addEventListener('online', function(e) {    alert("Your browser is online");}, false);window.addEventListener('offline', function(e) {    alert("Your browser is offline");}, false);*/function refreshAds() {    // Get all the slots    var allSlots = googletag.pubads().getSlots();    // Select the slots that are on the current page based on their dom Id    for (var i=0; i<allSlots.length; ++i){        googletag.pubads().refresh(allSlots[i]);    }}/*jQuery(document).on("pagechange", function($) {    initSidr(jQuery);    fetchMainMenuCategoryArticles(jQuery);    closeSidrOnResize(jQuery);    animateNewsFeed(jQuery);    makeArticleImagesResponsive(jQuery);    detectClient(jQuery);});*/